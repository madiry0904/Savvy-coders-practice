function sayHello(name, greeting) {
  return `Hello ${name} ${greeting}`;
}

console.log(sayHello("Madi", "How are you today?"));
//Simply create some variables using const and let.
//Give at least 3 strings and 3 numbers.
//In the case of let display that you understand
//how to reassign variables by reusing the
//same variable identifier (name).

let husbandName = "George";
husbandName = "Jack";

let favoriteFruit = "apples";
let favoriteActivity = "hiking";

const age = 23;
const nieceAge = 2 + 5;
const apartmentAge = 0;
